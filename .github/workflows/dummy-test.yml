name: dummy-test

on:
  pull_request:
  release:
    types:
      - edited
      - created
defaults:
  run:
    shell: bash
    
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - name: Message Echo Test
      id: message-echo-test
      run: |
          echo "$(git show -s --format=%s)" 
